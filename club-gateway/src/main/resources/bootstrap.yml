spring:
  application:
    name: club-gateway-dev
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: 192.168.56.1:8848
        prefix: ${spring.application.name}
        group: DEFAULT_GROUP
        file-extension: yaml
        long-poll-timeout: 30000
      discovery:
        enabled: true
        server-addr: 127.0.0.1:8848